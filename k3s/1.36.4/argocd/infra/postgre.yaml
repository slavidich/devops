apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: postgre
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/slavidich/devops.git
    targetRevision: main
    path: k3s/1.36.4/infra/postgre

  destination:
    server: https://kubernetes.default.svc
    namespace: postgres

  syncPolicy:
    automated:
      prune: true
      selfHeal: true